# AGENTS.md

This project's agent guidance lives in **[CLAUDE.md](CLAUDE.md)** — read it
first. It is the single source of truth for repository orientation, the
task→doc routing table, architectural invariants, conventions, and
build/test/run commands. This file exists only so agents that look for
`AGENTS.md` find the same guidance; do not duplicate content here.
